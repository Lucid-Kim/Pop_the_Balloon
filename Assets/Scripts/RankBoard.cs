using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RankBoard : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        UIManager.Inst.ShowScore();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
