using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParticleManager : Singleton<ParticleManager>
{
    [SerializeField] GameObject shitBalloonEffect;
    [SerializeField] GameObject flowerBalloonEffect;
    [SerializeField] GameObject seedBalloonEffect;
    [SerializeField] GameObject sunBalloonEffect;
    [SerializeField] GameObject sunLightEffect;
    [SerializeField] GameObject waterBalloonEffect;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
